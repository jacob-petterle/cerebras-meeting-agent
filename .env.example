# Cerebras (Gemma-4) — https://api.cerebras.ai/v1 — confirm `gemma-4-31b` shows in GET /v1/models
CEREBRAS_API_KEY=

# Cursor Agent SDK (sub-agent) — minted at cursor.com/dashboard/integrations (crsr_...)
CURSOR_API_KEY=
